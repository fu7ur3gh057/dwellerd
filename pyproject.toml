[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["server"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
filterwarnings = [
    "ignore::DeprecationWarning:taskiq.*",
]
