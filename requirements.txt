httpx[socks]>=0.27
pyyaml>=6.0
psutil>=5.9
asyncpg>=0.29
rich>=13.7
questionary>=2.0
fastapi>=0.115
uvicorn[standard]>=0.32
taskiq>=0.11
sqlmodel>=0.0.22
aiosqlite>=0.20
bcrypt>=4.0
PyJWT>=2.8

# Web terminal — PAM auth against system users. Pip-installable wrapper
# around libpam.so.0 (libpam0g on debian/ubuntu, pam on rhel/fedora —
# the host lib must be present, the python package is just a ctypes binding).
python-pam>=2.0
six>=1.16

# Phase-3+ extras: brought in early so `pip install -r` doesn't refetch
# when the broker switches from InMemory to Redis or sockets ship.
redis>=5.0
python-socketio>=5.11

# Telegram bot (bot/ package — interactive companion to the monitoring daemon)
aiogram>=3.13

# dev / test
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21
