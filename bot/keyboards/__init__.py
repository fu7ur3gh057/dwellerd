"""Keyboard builders.

One file per logical group of keyboards (e.g. main_menu.py, alerts.py).
Each function returns an `InlineKeyboardMarkup` or `ReplyKeyboardMarkup`
ready for `message.answer(..., reply_markup=...)`.
"""
